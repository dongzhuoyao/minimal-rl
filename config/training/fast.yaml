# @package training

# Fast training config for quick testing
num_epochs: 10
batch_size: 2
num_samples_per_prompt: 2
num_steps: 10
eval_num_steps: 10
learning_rate: 0.001
clip_range: 0.0001
beta: 0.0
eval_freq: 2
max_grad_norm: 1.0
device: "cpu"
