# @package training

# GPU-optimized training config
num_epochs: 50
batch_size: 8
num_samples_per_prompt: 8
num_steps: 20
eval_num_steps: 20
lr: 1e-3
clip_range: 0.0001
beta: 0.0
eval_freq: 5
max_grad_norm: 1.0
device: "cuda"
