# @package _global_

defaults:
  - model: default
  - training: default
  - dataset: default
  - paths: default

# Run name for logging and outputs
run_name: "flowgrpo_tutorial"

# Random seed for reproducibility
seed: 42
